# application-db.yaml
spring:
  config:
    activate:
      on-profile: db

  datasource:
    url: jdbc:postgresql://localhost:5432/usersdb
    username: postgres
    password: 0926
    driver-class-name: org.postgresql.Driver
